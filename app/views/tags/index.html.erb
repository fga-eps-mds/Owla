<h1>Tag</h1>
<td><%= link_to 'New Tag', new_question_tag_path %>
<br></br>
<table>
  <thead>
    <tr></tr>
  </thead>
    <th>Content</th>
    <th colspan="3"></th>
  <% @tags.each do |tag| %>
    <tr>
      <td><%= tag.content%></td>
      <td><%= link_to 'Show', tag %></td>
      <td><%= link_to 'Edit', edit_tag_path(tag) %></td>
      <td><%= link_to 'Delete', tag, method: :delete, data: {confirm: 'Deseja excluir?' } %></td>
      <td></td>
    </tr>
  <% end %>
</table>
<br>
