<h1>Edit Question</h1>
<% if flash[:alert] %>
    <div class="notice"><%= flash[:alert] %></div>
<% end %>
<%= form_for @question do |m| %>
	<%= m.label :content, "Name" %>
	<%= m.text_field :content %>
	<br>
	
	<%= m.submit "Edit Question" %>
<% end %>