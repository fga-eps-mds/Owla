<h1> Room </h1>

<table>
  <thead>
    <th>Name</th>
    <th>Topic</th>
  </thead>
  <tr>
    <td><%= @room.name%></td>
    <td><%= image_tag(@room.avatar.url(:thumb)) %></td>
    <%= link_to 'Cadastrar em sala', rooms_signup_path(id: @room.id), method: :post%>
  </tr>
</table>
